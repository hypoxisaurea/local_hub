from typing import Any


async def search_community_posts(message: str) -> list[dict[str, Any]]:
    # Hook for the backend team: replace this with DB search when community posts exist.
    return []
